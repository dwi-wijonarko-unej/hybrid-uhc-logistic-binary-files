# Hybrid Unimodular Hill + Logistic Map XOR Scheme for Binary Files

Experimental evaluation framework for the article
*"Experimental Evaluation of a Hybrid Unimodular Hill and Logistic Map XOR
Scheme for Binary Files"*.

The project implements a file cipher that treats **any file as raw bytes** and
combines two operations per 256-byte block:

1. a **Hill transform** with a 16 × 16 unimodular key matrix over Z/256Z, and
2. a byte-wise **XOR** with a keystream generated by a **logistic map**.

Decryption reproduces the original file bit-for-bit. A reproduction pipeline
measures runtime, byte statistics, and exact reconstruction for a set of test
files and emits all raw measurements, tables, and figures.

> "Any file" means the byte-level transform does not parse file-format
> semantics. The evaluation evidence in this repository covers only the file
> types and samples listed in the generated manifests; it does not establish
> functional behavior for all possible formats, encrypted containers,
> streaming inputs, or malformed files.

> ## ⚠️ Security warning — read this first
>
> This is **not** modern authenticated encryption and must never be used to
> protect real data. It is a *proof-of-concept* built to evaluate
> **reversibility, byte statistics, and runtime** of the studied scheme.
> Specifically:
>
> - No security claims are made against known-plaintext, chosen-plaintext,
>   or any modern cryptanalytic attack. The Hill stage is linear and the
>   logistic map is deterministic in IEEE-754 double precision.
> - There is **no per-file nonce or diversification** in this baseline.
>   Every file operation starts from the same configured logistic initial
>   condition (`logistic_x0`) after the same warm-up, so the **same
>   keystream prefix is reused across files and runs** (the classic
>   two-time-pad scenario), and the same key matrix encrypts every file.
>   This is a deliberate baseline experimental decision and a documented
>   security limitation — not a production design.
> - There is **no integrity or authentication** (no MAC/tag); ciphertexts are
>   malleable.
> - Key material lives in a plaintext YAML file.
> - Logistic maps in finite precision have short cycles and orbit
>   collisions; the quantization `floor((x*1000) mod 256)` does not produce
>   a uniform byte distribution.

## Contents

- [Purpose and scope](#purpose-and-scope)
- [Algorithm specification](#algorithm-specification)
- [Project layout](#project-layout)
- [Requirements and setup](#requirements-and-setup)
- [Usage](#usage)
- [Configuration parameters](#configuration-parameters)
- [Output files](#output-files)
- [Metric definitions](#metric-definitions)
- [Benchmark protocol](#benchmark-protocol)
- [Limitations](#limitations)
- [License](#license)

## Purpose and scope

Evaluate, reproducibly and from a single YAML configuration:

- **exact reversibility** of the encrypt→decrypt roundtrip (SHA-256 and MD5),
- **byte statistics** of plaintext vs ciphertext (Shannon entropy,
  adjacent-byte correlation, chi-square against uniformity),
- **runtime** (encryption/decryption time and throughput per file size),
- **ciphertext expansion** (expected to be exactly zero).

Out of scope: any claim of cryptographic security. Out of scope: AES, RSA, or
any other standard cipher — the implementation uses only the studied
unimodular Hill transform and the logistic-map XOR.

## Algorithm specification

All parameters below are read from `config/experiment_config.yaml`; there are
no hidden defaults and no interactive input.

### Key material (unimodular 16 × 16 matrix)

1. Generate a logistic byte sequence of length
   `sequence_length = n(n-1)/2 + (n-1)` = **135** for `n = 16`
   (same recurrence and quantization as the keystream below, including the
   warm-up).
2. Initialize `M = I_n` (integer identity).
3. Fill the strict upper triangle in row-major order:
   `M[i, j] = sequence[index]`, `index += 1` for all `i < j`.
4. Fill the lower part with determinant-preserving elementary row additions:
   for `row_i` in `1..n-1`: `multiplier = sequence[index]`,
   `M[row_i, :] = (M[row_i, :] + multiplier * M[0, :]) mod 256`, `index += 1`.
   Because the construction starts at the identity and only applies
   `R_i ← R_i + a·R_0`, the determinant stays exactly 1, so
   `det(M) ≡ 1 (mod 256)` and M is invertible modulo 256.
5. The inverse `M⁻¹` is computed **exactly** with Gauss–Jordan elimination
   over Z/256Z (see below) and verified:
   `(M @ M⁻¹) mod 256 == I` **and** `(M⁻¹ @ M) mod 256 == I`.

Both matrices are exported to `results/configuration/key_matrix.csv` and
`results/configuration/key_matrix_inverse_mod256.csv`.

### Modular matrix inverse (composite modulus 256)

`numpy.linalg.inv` is **never** used (floating point, meaningless modulo
256). The elimination:

- pivots only on units (`gcd(pivot, 256) == 1`), swapping rows when needed;
- raises a clear error when no unit pivot exists (matrix singular mod 256);
- inverts pivots with the **extended Euclidean algorithm**;
- runs on signed 64-bit integers with an explicit `% 256` after every update,
  so no implicit uint8 wrap-around can occur before reduction.

### Logistic-map keystream

- Recurrence: `x_(t+1) = r · x_t · (1 − x_t)` in IEEE-754 binary64
  (Python `float`).
- **1000 warm-up iterations** are discarded before the first byte.
- Quantization: `k_i = floor((x_i · 1000) mod 256)`, stored as `uint8`.
- The generator is **stateful and continuous within one encryption or
  decryption operation**: the orbit is not reset between blocks of the same
  file; full blocks consume keystream bytes `[0, n_full·256)` and the tail
  consumes the remaining bytes. For reproducibility, each file operation
  (and each benchmark run) initializes a **fresh generator** from the
  configured `logistic_x0` and performs the warm-up once. Consequently, the
  same keystream prefix is reused across files and runs in this baseline
  (documented as a security limitation below).
- Identical parameters always produce byte-identical keystreams (unit-tested).

### Encryption

1. Read the file as raw bytes.
2. Split into full 256-byte blocks plus a residual tail.
3. For each full block: reshape row-major to 16 × 16 (`P_i`), compute
   `H_i = (M @ P_i) mod 256`, serialize `H_i` row-major to 256 bytes, and
   XOR with the next 256 keystream bytes.
4. The residual tail (< 256 bytes) never passes through the Hill matrix
   (incomplete dimensions); it is only XORed: `C_tail = P_tail ⊕ K_tail`.
5. Ciphertext length **always equals** plaintext length (zero expansion).
   Output is written as a raw binary file.

### Decryption

Symmetric, with the identical parameters and the same initial logistic
setting: XOR with the identical keystream first, then apply `M⁻¹` to every
full block (`P_i = (M⁻¹ @ H_i) mod 256`); the tail is XORed back. Exact
reconstruction is verified with SHA-256 (MD5 is additionally recorded for
legacy compatibility; SHA-256 is authoritative).

## Project layout

```
hybrid-uhc-logistic-binary-files/
├── README.md
├── LICENSE                        # MIT
├── requirements.txt               # pinned core dependencies
├── requirements-benchmark.txt     # pinned optional extras (real-format benchmark)
├── environment.yml                # conda environment (Python 3.10)
├── pyproject.toml                 # packaging + pytest config
├── .gitignore
├── config/
│   └── experiment_config.yaml     # every experimental parameter
├── src/hybrid_crypto/
│   ├── __init__.py
│   ├── config.py                  # YAML loading + validation (no hidden defaults)
│   ├── logistic_map.py            # LogisticMapKeystream (stateful, continuous)
│   ├── matrix_generator.py        # unimodular key matrix + inverse verification
│   ├── modular_algebra.py         # exact Gauss-Jordan inverse over Z/256Z
│   ├── cipher.py                  # encrypt/decrypt bytes + file helpers
│   ├── metrics.py                 # entropy, correlation, chi-square, throughput
│   ├── experiment.py              # benchmark protocol orchestration
│   ├── io_utils.py                # paths, hashing, CSV/JSON, logging
│   └── reporting.py               # tables, key-material report, figures
├── scripts/
│   ├── encrypt_file.py            # encrypt one file (config-driven)
│   ├── decrypt_file.py            # decrypt one file (+ optional verification)
│   ├── generate_synthetic_test_files.py  # 12 deterministic placeholders
│   ├── benchmark_real_files.py    # revised legacy benchmark on real DOCX/PDF/JPEG
│   └── reproduce_experiments.py   # full experiment reproduction
├── tests/                         # pytest unit + roundtrip tests
├── data/
│   ├── README.md                  # where real test files / manifests go
│   ├── input/                     # plaintext test files (not committed)
│   └── manifest/                  # input manifests/checksums (not committed)
└── results/                       # all generated artifacts (not committed)
    ├── configuration/  ├── ciphertexts/  ├── decrypted/
    ├── raw/  ├── tables/  ├── figures/  └── logs/
```

- Python **3.10** (also runs on newer CPython; the code avoids 3.11+-only syntax).
- Core runtime dependencies: NumPy, SciPy, Matplotlib, and PyYAML; pytest is
  required for the test suite. The optional real-format benchmark
  (`scripts/benchmark_real_files.py`) additionally requires `python-docx`
  and `Pillow` for test-file **generation only** — nothing is ever
  auto-installed; install the pinned extras separately:

```bash
pip install -r requirements.txt -r requirements-benchmark.txt
```

**venv (pip):**

```bash
python3.10 -m venv .venv
source .venv/bin/activate          # Windows: .venv\Scripts\activate
pip install -r requirements.txt
```

`requirements.txt` pins the Python 3.10 baseline (`numpy==1.26.4`,
`scipy==1.11.4`, `matplotlib==3.8.4`, `PyYAML==6.0.1`, `pytest==8.1.1`) and
contains environment markers that resolve compatible wheels on newer
interpreters. For the same input files, configuration, implementation
revision, and an IEEE-754 binary64 execution path, ciphertexts, hashes, and
byte-level statistics are deterministic. Runtime and throughput
measurements are hardware-, OS-, and system-load-dependent and are **not**
expected to be identical across machines. The implementation has been
validated on CPython 3.14 (written to be 3.10-compatible); bit-identical
logistic sequences on non-CPython interpreters or non-IEEE-754 platforms
are not guaranteed.

**conda:**

```bash
conda env create -f environment.yml
conda activate hybrid-uhc-logistic
```

The experiment environment (interpreter, package versions, OS, CPU) should be
recorded alongside any published numbers — see *Limitations*.

## Usage

Run everything from the repository root.

**Unit tests:**

```bash
python -m pytest
```

**Generate the 12 deterministic synthetic placeholder files**
(3 DOCX-like / PDF-like / JPEG-like types × 4 sizes, from
`synthetic_files` in the config):

```bash
python scripts/generate_synthetic_test_files.py --config config/experiment_config.yaml
```

**Reproduce the full experiment** (manifest, raw measurements, byte
statistics, reconstruction checks, key-material report, summary table, and
all figures under `results/`):

```bash
python scripts/reproduce_experiments.py --config config/experiment_config.yaml
```

If `data/input/` is empty, the synthetic files are generated automatically
first (`experiment.generate_synthetic_files_if_missing: true`).

**Encrypt a single file** (metadata JSON optional):

```bash
python scripts/encrypt_file.py \
    --config config/experiment_config.yaml \
    --input data/input/synthetic_pdf_128KB.bin \
    --output results/ciphertexts/manual_pdf.enc \
    --metadata results/ciphertexts/manual_pdf.meta.json
```

**Decrypt a single file and verify exact reconstruction:**

```bash
python scripts/decrypt_file.py \
    --config config/experiment_config.yaml \
    --input results/ciphertexts/manual_pdf.enc \
    --output results/decrypted/manual_pdf.dec \
    --verify-original data/input/synthetic_pdf_128KB.bin \
    --metadata results/decrypted/manual_pdf.meta.json
```

The decrypt script exits non-zero if `--verify-original` is given and the
reconstruction does not match byte-for-byte.

### Revised legacy benchmark on real DOCX / PDF / JPEG files

`scripts/benchmark_real_files.py` is the direct revision of the legacy
monolithic benchmark program (Unimodular Hill + Logistic Map + **Shift
Cipher**). The revision is structural, not cosmetic:

| Legacy program | Revised program |
|---|---|
| Shift Cipher on the tail | **removed**; no Shift Cipher anywhere |
| Tail-only chaotic XOR with a second seed (`x0 + 0.12345`) | one continuous logistic keystream XORs the **entire** Hill output (full blocks *and* tail) from `x0 = 0.73911` |
| Hill over the whole file as one n × columns matrix | Hill per **16 × 16 (256-byte) row-major block** |
| Heuristic elimination for the inverse | exact Gauss–Jordan over Z/256Z with unit pivots + extended Euclid, verified in both product directions |
| Interactive passwords (`PWD1`/`PWD2`) | frozen `CipherParameters` dataclass (constructor/CLI configurable) |
| Keystream re-ran the 1000-iteration warm-up on every call, with NaN fallbacks | stateful stream, warm-up once per operation, strict parameter validation |
| MD5-only integrity | SHA-256 primary (byte-compare confirmed), MD5 secondary |

It keeps the legacy workflow: generates format-valid base documents (real
DOCX via python-docx, hand-built PDF with coherent xref/trailer, real JPEG
via Pillow) and pads each to its target size with filler inserted in
format-legal locations (extra ZIP entries, extra PDF stream objects, JPEG
COM segments), then runs the benchmark loop and prints TABLE 1 (entropy /
adjacent-byte correlation), TABLE 2 (times and throughputs), TABLE 3
(integrity), exports `benchmark/summary_results.csv` and
`benchmark/source_manifest.csv` (SHA-256 per input file), and renders the
plaintext/ciphertext histogram figure. The cryptographic core is imported
from `hybrid_crypto`, so it runs exactly the algorithm covered by the unit
tests (bounded-memory chunked processing for large files). `python-docx`
and `Pillow` are needed **only** for test-file generation, never for the
crypto; they are pinned in `requirements-benchmark.txt` and are **not**
auto-installed.

```bash
# full default set: DOCX/PDF/JPEG at 1, 10, 50, 100 MB (12 files)
python scripts/benchmark_real_files.py

# quick run: one size class only (3 files)
python scripts/benchmark_real_files.py --target-sizes-mb 1

# generate the deterministic dataset without benchmarking, or reuse existing files
python scripts/benchmark_real_files.py --generate-only
python scripts/benchmark_real_files.py --skip-generate
python scripts/benchmark_real_files.py --regenerate
```

Reproducibility of the generated dataset:

- **Default (`--padding-source seeded`, seed via `--seed`)**: the filler is a
  fixed-seed SplitMix64 stream, so regenerating produces **byte-identical**
  files for the same seed, sizes, and generator revision. This is the mode to
  use when the benchmark dataset must be reproducible.
- **`--padding-source urandom`**: matches the legacy program's padding, which
  is *not* bit-reproducible — re-running the generator produces a different
  dataset, so the generated files must be retained unchanged and audited via
  `benchmark/source_manifest.csv` (SHA-256 per file). A `urandom` dataset is
  not, by itself, a reproducibility source for reported numbers.

The cryptography itself is deterministic for a given input file and parameter
set. Timings include file I/O, use 1 MB = 1,048,576 bytes, and are
hardware-dependent.

## Configuration parameters

`config/experiment_config.yaml` — the loader raises `ConfigError` on any
missing key; there are no code-side defaults for experimental parameters.

| Key | Default | Meaning |
|---|---|---|
| `experiment.name` | `hybrid-uhc-logistic-xor-baseline` | Label written to logs. |
| `experiment.input_dir` | `data/input` | Plaintext test files (all regular files, sorted). |
| `experiment.output_dir` | `results` | Root for all artifacts. |
| `experiment.generate_synthetic_files_if_missing` | `true` | Auto-generate synthetic files when input dir is empty. |
| `experiment.histogram_example_filename` | `synthetic_jpeg_256KB.bin` | Input used for the byte-frequency histogram figure. |
| `cipher.matrix_dimension` | `16` | n for the n × n key matrix. |
| `cipher.block_size` | `256` | Bytes per Hill block; must equal `matrix_dimension²`. |
| `cipher.modulus` | `256` | Byte algebra modulus (must be 256). |
| `cipher.logistic_r` | `3.923` | Logistic control parameter (0 < r ≤ 4). |
| `cipher.logistic_x0` | `0.73911` | Initial condition (0 < x0 < 1). |
| `cipher.warmup_iterations` | `1000` | Discarded iterations before the first byte. |
| `cipher.quantization_rule` | `k_i = floor((x_i*1000) mod 256)` | Documentation string echoed into the key report. |
| `cipher.numeric_precision` | IEEE-754 binary64 | Documentation string echoed into the key report. |
| `benchmark.timing_warmup_runs` | `1` | Warm-up runs per file, excluded from statistics. |
| `benchmark.timing_repetitions` | `5` | Measured runs recorded in `raw_measurements.csv`. |
| `benchmark.include_file_io_in_timing` | `true` | Time read+transform+write (`false` = transform only). |
| `benchmark.megabyte_bytes` | `1048576` | Fixed MB convention (1 MB = 1 MiB = 1024² bytes). |
| `synthetic_files.*` | see file | Seed, types, sizes, labels, output/manifest dirs. |

## Output files

Under `results/` after `reproduce_experiments.py`:

| Path | Content |
|---|---|
| `configuration/used_config.yaml` | Verbatim copy of the config used for the run. |
| `configuration/key_matrix.csv` | The 16 × 16 unimodular key matrix (headerless integer CSV). |
| `configuration/key_matrix_inverse_mod256.csv` | Its exact modular inverse. |
| `configuration/key_material_report.json` | Parameters, sequence length, matrix SHA-256s, inverse verification result, nonce/keystream-reuse policy, parameter ID. |
| `raw/file_manifest.csv` | `file_id, filename, file_type, exact_size_bytes, nominal_size_label, sha256_plaintext`. |
| `raw/raw_measurements.csv` | One row per measured run: timings, throughputs, sizes, expansion, SHA-256s, `reconstruction_match`. |
| `raw/byte_statistics.csv` | Entropy (plain/cipher), adjacent-byte correlation (plain/cipher), ciphertext chi-square statistic and p-value. |
| `raw/reconstruction_checks.csv` | MD5 (secondary) and SHA-256 (primary) of plaintext vs decrypted, `exact_match`. |
| `tables/summary_statistics.csv` | Per file: mean, sample std, median, min, max of every timed series. |
| `figures/fig_entropy_comparison.png` | Grouped bars: plaintext vs ciphertext entropy. |
| `figures/fig_adjacent_byte_correlation.png` | Grouped bars: adjacent-byte Pearson r. |
| `figures/fig_throughput_vs_file_size.png` | Mean enc/dec throughput vs file size. |
| `figures/fig_ciphertext_expansion.png` | Expansion bytes per file (0 = length preserving). |
| `figures/fig_byte_frequency_histogram.png` | 256-bin byte histograms of the configured example file. |
| `ciphertexts/`, `decrypted/` | Raw binary artifacts per input file (`<file_id>_<name>.enc` / `.dec`). |
| `logs/experiment.log` | Concise run log (no matrices or large dumps are printed). |

`matrix_sha256` in the key report is the SHA-256 over the exact bytes of the
referenced CSV artifact.

## Metric definitions

- **Shannon entropy**: `H = −Σ p_i log2(p_i)` over the 256-value byte
  distribution, in bits/byte.
- **Adjacent-byte correlation**: Pearson r between `data[:-1]` and
  `data[1:]`; `nan` for constant data (zero variance).
- **Chi-square vs uniformity**: `Σ (O_i − N/256)² / (N/256)` with 255 degrees
  of freedom; computed on the ciphertext.
- **Throughput**: `file_size_megabytes / elapsed_seconds`, with
  **1 MB = 1,048,576 bytes** (fixed; set `benchmark.megabyte_bytes` to
  `1000000` for the decimal convention, consistently everywhere).
- **Ciphertext expansion**: `ciphertext_size − plaintext_size` in bytes
  (and as % of plaintext size); expected to be exactly 0.
- **Exact reconstruction**: SHA-256(plaintext) == SHA-256(decrypted),
  additionally confirmed by direct byte comparison. MD5 is recorded only as
  a legacy-compatibility secondary checksum.

## Benchmark protocol

- Per file: `timing_warmup_runs` untimed-in-statistics warm-up runs first,
  then `timing_repetitions` measured runs; **every measured run is a row in
  `raw_measurements.csv`** and the summary adds mean / sample standard
  deviation (ddof = 1) / median / min / max per timed series.
- `include_file_io_in_timing: true` times read + transform + write; `false`
  times only the in-memory transform (artifacts are still written outside
  the measured interval).
- Timings are wall-clock and **hardware/OS/interpreter dependent**; they are
  not comparable across platforms and must not be presented as
  cross-platform benchmarks.

## Limitations

1. **Not secure cryptography** — see the warning at the top. No resistance
   to known-plaintext/chosen-plaintext attacks is claimed or expected; the
   linear Hill stage leaks structure under such attacks.
2. **Keystream prefix reuse across files and runs**: every file operation
   initializes a fresh logistic generator from the same configured
   `logistic_r`/`logistic_x0`/warm-up, so every file and every run is
   encrypted with the same keystream prefix — the classic two-time-pad
   scenario. Per-file nonce diversification is explicitly disabled in this
   baseline and flagged in `key_material_report.json`
   (`per_file_nonce_or_diversification: false`).
3. **Matrix/keystream overlap (independent generators, Model A)**: the
   key-matrix generator and the file-keystream generator are initialized
   independently with the same configured logistic parameters, so the first
   135 quantized bytes used to construct the key matrix are **also reused
   as the first 135 bytes of the file keystream** (pinned by a unit test).
   This deterministic overlap is an intentional baseline property and a
   documented security limitation.
4. **Finite-precision logistic map**: IEEE-754 orbits can enter short
   cycles and the quantized bytes are not uniformly distributed.
5. **Runtime results** are hardware-dependent (see *Benchmark protocol*).
6. **Synthetic test files** (from `generate_synthetic_test_files.py`) are
   byte-level placeholders that carry recognizable format signatures (magic
   bytes) but are **not guaranteed to be standards-conformant** DOCX, PDF,
   or JPEG documents — applications may reject them. They reproduce the
   byte-processing workflow only, not application-level format validity or
   the article's numeric byte statistics. Supply the original files in
   `data/input/` to reproduce the article's numbers.
7. Large inputs are slower than native ciphers because the keystream is a
   pure-Python orbit loop; this is part of what the experiment measures.

## License

Code is released under the MIT License — see [LICENSE](LICENSE). Dataset
files, generated experimental outputs (tables, figures, raw measurements),
and any third-party test materials are **not** automatically covered by the
code license; their terms are described in `data/README.md`. If you publish
a frozen dataset or archived artifacts, assign them an explicit data license
(e.g., CC BY 4.0 or CC0) separately.

### Versioning and archival

Revisions intended to support a manuscript should be frozen as an annotated
git tag (e.g., `v0.1.0`) and archived in a DOI-minting repository (Zenodo or
similar). Together, the tag, `results/configuration/used_config.yaml`, and
`results/raw/file_manifest.csv` identify the exact code, parameters, and
dataset behind any reported number.
